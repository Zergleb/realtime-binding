apply plugin: 'java'
apply plugin: 'eclipse'
apply plugin: 'maven-publish'

sourceCompatibility = 1.6
version = '0.1'

repositories {
    mavenCentral()
}

uploadArchives {
    repositories {
       flatDir {
           dirs 'repos'
       }
    }
}
