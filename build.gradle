apply plugin: 'java'
apply plugin: 'eclipse'

sourceCompatibility = 1.6
version = '0.1'

repositories {
    mavenCentral()
}

dependencies {
}

uploadArchives {
    repositories {
       flatDir {
           dirs 'repos'
       }
    }
}
